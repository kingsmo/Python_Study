매개변수
함수를 정의할 때 사용하는 이름
실행 인자
함수를 실행할 때 넘기는 변수, 값
매개변수와 실행 인자
매개변수와 실행 인자의 개수는 동일해야 한다.
여러 개일 경우 쉼표로 구분
def print_round(number):    # 함수의 정의
    rounded = round(number)
    print(rounded)  

print_round(4.6)        # 함수의 호출
print_round(2.2)
