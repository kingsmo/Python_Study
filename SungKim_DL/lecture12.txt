12 - 1 RNN

우리는 하나의 단어가 아니라 sequential한 말을 이해해야 한다.

이전 계산이 다음 계산의 영향을 미치는 경우
x -> rnn -> y
rnn은 이전 state가 다음 state의 인풋이 된다.
연산하는 펑션은 하나기 때문에 rnn을 여러개로 표시 하지 않는다.

W_hh, W_hx, W_hy도 다 동일한 값

랭귀지모델링
스피치 인식
머신 번역
다양한 시퀀셜 데이터에서 쓰임
multi layer, 다중출력, 한가지 값출력 등 여러가지에서 쓰임
